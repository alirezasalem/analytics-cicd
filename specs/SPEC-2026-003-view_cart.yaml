[spec-generator] Calling Claude API...
[spec-generator] ✓ Generated SPEC-2026-001 — undefined
spec_id: SPEC-2026-001
generated_by: claude-api
status: draft
spec_version: 1.0.0
feature_brief: FB-ecommerce-tracking
owner: Commerce
created_at: 2026-04-17
updated_at: 2026-04-17

events:
  - name: view_cart
    trigger: "User navigates to /cart page with at least one item in cart"
    priority: P1
    parameters:
      - name: value
        type: currency
        required: true
        currency_param: currency
        example: 89.97
      - name: currency
        type: string
        required: true
        example: "EUR"
      - name: item_count
        type: number
        required: true
        example: 3
      - name: items
        type: array
        required: true
        example: 
          - item_id: "SKU123"
            item_name: "Wireless Headphones"
            price: 49.99
            quantity: 1
            item_brand: "TechCorp"
            item_category: "Electronics"
      - name: coupon
        type: string
        required: false
        example: "SAVE10"
    notes: "Fire once per page load only. Use sessionStorage flag cart_viewed_{session_id} to prevent duplicate fires on SPA re-renders. Do not fire if cart is empty. Clear flag on session end."
