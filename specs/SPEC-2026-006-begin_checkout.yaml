spec_id: SPEC-2026-006-begin_checkout
version: 1.0.0
status: draft
owner: Analytics Team
feature_brief_ref: FB-006
generated_by: claude-api-agent
created_at: 2026-05-10

events:
  - name: begin_checkout
    trigger:
      action: button_click
      selector: "NEEDS_CLARIFICATION: checkout button selector not specified"
      page_path: "NEEDS_CLARIFICATION: cart page path not specified"
    parameters:
      currency: "DL - currency"
      value: "DL - cart_total"
      items: "DL - cart_items"
    dataLayer:
      event: begin_checkout
      event_category: ecommerce
      event_label: checkout_start
      currency: "{{DL - currency}}"
      value: "{{DL - cart_total}}"
      items: "{{DL - cart_items}}"
    ga4_mapping:
      event_name: begin_checkout
      parameters:
        currency: "{{DL - currency}}"
        value: "{{DL - cart_total}}"
        items: "{{DL - cart_items}}"
    gtm_notes: "Standard GA4 ecommerce event. Ensure cart_items array contains item_id, item_name, price, quantity for each item."

acceptance_criteria:
  - "Event fires once per checkout button click"
  - "Currency is valid ISO 4217 code"
  - "Value matches total cart value including tax and shipping"
  - "Items array contains all required fields for each cart item"
  - "Event appears in GA4 DebugView with correct ecommerce parameters"
