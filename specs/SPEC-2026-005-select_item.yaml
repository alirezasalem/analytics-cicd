spec_id: SPEC-2026-005-select_item
version: 1.0.0
status: draft
owner: NEEDS_CLARIFICATION: owner not specified in feature brief
feature_brief_ref: FB-005
generated_by: claude-api-agent
created_at: NEEDS_CLARIFICATION: timestamp will be set by pipeline

events:
  - name: select_item
    trigger: "Product Card - Click - Select Item"
    parameters:
      product_id: "DL - product_id"
      product_name: "DL - product_name"
      price: "DL - price"
      brand: "DL - brand"
      category: "DL - category"
      list_name: "DL - list_name"
      list_position: "DL - list_position"
      currency: "DL - currency"
    dataLayer:
      event: select_item
      event_category: ecommerce
      event_label: "{{DL - product_name}}"
      product_id: "{{DL - product_id}}"
      product_name: "{{DL - product_name}}"
      price: "{{DL - price}}"
      brand: "{{DL - brand}}"
      category: "{{DL - category}}"
      list_name: "{{DL - list_name}}"
      list_position: "{{DL - list_position}}"
      currency: "EUR"
    ga4_mapping:
      event_name: select_item
      custom_dimensions:
        cd_list_name: "{{DL - list_name}}"
        cd_list_position: "{{DL - list_position}}"
        cd_brand: "{{DL - brand}}"
        cd_category: "{{DL - category}}"
    gtm_notes: "Fire on all product card clicks using Click Element trigger. Target clickable product card containers across category pages, search results, homepage carousels, and recommendation widgets."

acceptance_criteria:
  - "Event fires once per product card click, not on page load or navigation completion"
  - "list_name accurately reflects page context (e.g. 'Homepage - Recommendations', 'Category - Running Shoes', 'Search Results')"
  - "list_position uses 1-based indexing with first product = 1"
  - "All product data populated from dataLayer before GTM fires"
  - "Currency is always EUR as specified"
  - "Verified in GA4 DebugView across all product list contexts before merge"
