{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "."
      ],
      "description": "Read/write access to the analytics-cicd repo root. Used by the spec-generator agent to read conventions.yaml and write spec entries."
    },
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub API access for reading PRs, issues, and pushing spec files. Requires GITHUB_TOKEN env var."
    }
  }
}
