name: Deploy to Staging

on:
  workflow_run:
    workflows:
      - "Generate GTM Config"
    types:
      - completed
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Deploy step not yet configured"
